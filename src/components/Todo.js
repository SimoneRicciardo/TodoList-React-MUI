import React from "react";

const Todo = ({id,title,userId,completed}) => {
    return(
        <div>
           {/*  <span>{id}</span>
            <span>{title}</span>
            <span>{completed}</span> */}
        </div>
    )
} 

export default Todo